/** Route: `/dashboard/portfolio/extraction-rules` — how metrics are pulled from PDFs. */
import { ExtractionRulesView } from "@/components/portfolio-monitoring/ExtractionRulesView";

/** Shows the metric dictionary and extraction rule editor. */
export default function ExtractionRulesPage() {
  return <ExtractionRulesView />;
}
