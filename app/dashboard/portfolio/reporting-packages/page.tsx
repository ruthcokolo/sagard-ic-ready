/** Route: `/dashboard/portfolio/reporting-packages` — upload and manage report PDFs. */
import { ReportingPackagesView } from "@/components/portfolio-monitoring/ReportingPackagesView";

/** Shows reporting package uploads, batch ingest, and package table. */
export default function ReportingPackagesPage() {
  return <ReportingPackagesView />;
}
