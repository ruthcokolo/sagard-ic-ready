/** Route: `/dashboard/portfolio/settings` — portfolio monitoring preferences. */
import { PortfolioSettingsView } from "@/components/portfolio-monitoring/PortfolioSettingsView";

/** Shows validation toggles, demo reset, and monitoring settings. */
export default function PortfolioSettingsPage() {
  return <PortfolioSettingsView />;
}
