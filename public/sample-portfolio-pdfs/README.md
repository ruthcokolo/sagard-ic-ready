# Sample Portfolio PDFs

See the full documentation in the project-root folder:

**`sagard-ic-ready/sample-portfolio-pdfs/README.md`**

This `public/` copy is synced automatically when you run:

```bash
npm run generate:sample-pdfs
npm run generate:template-pdfs
```

**Company-formatted PDFs** are in this folder. **ICReady template PDFs** are in `icready-template/`.
